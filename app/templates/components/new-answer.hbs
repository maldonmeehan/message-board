{{#if addAnswer}}
  <form>
    <div class="form-group">
      <label for="name"> Name:</label>
      {{input id="name" value=name placeholder="your name"}}
    </div>
    <div class="form-group">
      <label for="content">Answer:</label>
      {{input id="content" value=content placeholder="your answer"}}
    </div>
    <button {{action 'saveAnswer'}}>Submit Answer</button>
  </form>
{{else}}
  <hr>
  <h3>Click bellow to answer this Question:</h3>
  <button {{action 'showAnswerForm'}} class="btn btn-info">Answer</button>
{{/if}}
